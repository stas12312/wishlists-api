info:
  name: Обновление данных
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/api/user"
  body:
    type: json
    data: |-
      {
          "name": "Станислав Руш",
          "username": "username",
          "image": "https://image.ru"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
