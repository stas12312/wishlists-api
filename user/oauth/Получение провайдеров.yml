info:
  name: Получение провайдеров
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/auth/oauth/providers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
