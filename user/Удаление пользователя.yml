info:
  name: Удаление пользователя
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{base_url}}/api/user"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
