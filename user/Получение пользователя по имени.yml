info:
  name: Получение пользователя по имени
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/api/user/:username"
  params:
    - name: username
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
