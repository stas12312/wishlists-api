info:
  name: Получение текущего пользователя
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/user/me"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
